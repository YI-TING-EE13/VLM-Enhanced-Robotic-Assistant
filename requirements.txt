# Core libraries for the VLM-Enhanced Robot Assistant

# ASR (Automatic Speech Recognition) dependencies
openai-whisper                  # OpenAI Whisper for speech-to-text conversion

# VLM (Vision-Language Model) dependencies
google-generativeai            # Google Gemini API for multimodal AI
pillow                         # PIL for image processing

# TTS (Text-to-Speech) dependencies
edge-tts                       # Microsoft Edge TTS for speech synthesis
playsound==1.2.2              # Audio playback (version 1.2.2 for Windows stability)

# Audio recording dependencies
sounddevice                    # Real-time audio recording from microphone
numpy                          # Numerical operations for audio processing
wave                           # WAV file format support

# General utilities
python-dotenv                  # Environment variable management for API keys

# Optional: Conda environment setup
# conda install python=3.10 -y
# conda install pytorch torchvision torchaudio -c pytorch -y
